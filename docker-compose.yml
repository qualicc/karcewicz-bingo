version: '3'
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "9500:80"
    volumes:
      - ./:/var/www/html
    environment:
      - APACHE_RUN_USER=#1000
